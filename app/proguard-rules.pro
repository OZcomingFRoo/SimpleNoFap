# Project-specific release shrinker rules.
#
# Keep this file intentionally small. Add rules only when a release build or runtime
# verification shows that R8 removed or renamed something the app needs.
